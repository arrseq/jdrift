{
  "name": "jdrift-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "build:js": "esbuild src/main.ts --bundle --platform=browser --minify --sourcemap --outfile=src/build/index.js --tsconfig=tsconfig.json"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1"
  },
  "dependencies": {
    "esbuild": "^0.24.0",
    "xbinser": "^1.0.1"
  }
}
